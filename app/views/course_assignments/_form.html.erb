<%= simple_form_for(@course_assignment) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
  </div>

  <div class="form-actions">
  	<%= f.association :course_session, label_method: :full_title, value_method: :id %>
  	<%= f.input :number %>
  	<%= f.input :title %>
    <%= f.input :description %>
    <%= f.input :format %>
    <%= f.input :due_date %>
    <%= f.button :submit %>
  </div>
<% end %>
