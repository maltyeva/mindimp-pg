<h1><%= @title %></h1>

<ul>
<% @responses.each do |b| %>
<li>
	<dt><%= link_to b.discussion_question.question + " -- " + b.discussion_question.source, discussion_question_discussion_response_path(b.discussion_question, b) %><dt>
		<dd><%= simple_format(b.response) %></dd>
		<dd>Submitted: <%= (b.created_at + 28800).strftime('%B %e, %Y %l:%M %p') %></dd>
		<hr/>
</li>
<% end %>
</ul>


</br>
<%= link_to "Back", user_path%>