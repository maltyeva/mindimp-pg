<%= form_for(current_user.book_lists.
                          build(book_id: @book.id),
                          remote: true) do |f| %>
  <div><%= hidden_field_tag :book_id, @book.id %></div>
  <%= f.submit "Add to my books", class: "btn btn-primary" %>
<% end %>